name: Deploy Clinic Automation Backend API

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: rocklimedev
          password: ${{ secrets.CLINIC_AUTOMATION_BACKEND_GHCR_TOKEN }}

      - name: Build and push API image
        run: |
          docker build --no-cache \
            -t ghcr.io/rocklimedev/clinic-automation-backend:latest \
            ./backend

          docker push ghcr.io/rocklimedev/clinic-automation-backend:latest

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: 66.116.204.81
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e

            cd /opt/apps/clinic-automation-backend

            echo "${{ secrets.CLINIC_AUTOMATION_BACKEND_GHCR_TOKEN }}" | docker login ghcr.io \
              -u rocklimedev --password-stdin

            docker compose pull backend

            docker compose up -d backend \
              --force-recreate \
              --remove-orphans

            docker image prune -f
